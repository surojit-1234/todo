import React from 'react'
import { INCREMENT } from './actions'

const userReducer = () => {
    const initialVal={
        count: 0
    }

   switch(state=initialVal, action){
       case INCREMENT
   }
}

export default userReducer